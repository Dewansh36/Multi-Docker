sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t dc/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true dc/react-test npm test

after_success:
  - docker build -t dc/multi-client ./client
  - docker build -t dc/multi-nginx ./nginx
  - docker build -t dc/multi-server ./server
  - docker build -t dc/multi-worker ./worker
  - docker login -u "dewansh05" -p "ybcRyqunT!!yp9R" docker.io
  - docker push dewansh05/complex:dc/multi-client
  - docker push dewansh05/complex:dc/multi-nginx
  - docker push dewansh05/complex:dc/multi-server
  - docker push dewansh05/complex:dc/multi-worker
